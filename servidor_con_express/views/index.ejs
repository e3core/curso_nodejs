<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1><%= title %></h1>
    <% if(showmessage){ %>
        <%= message %>
    <% } else { %>
        Este es un mnesaje por defecto
    <% } %>

    <% for(let i=0;i < ites.length;i++){ %>
        <h1><%= ites[i] %></h1>
        <% } %>
</body>
</html>